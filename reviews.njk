---
layout: base.njk
title: AI Tool Reviews
description: In-depth analysis of AI tools, including features, pros, cons, and use cases
---

<div class="reviews-page">
  <header class="page-header">
    <h1>AI Tool Reviews</h1>
    <p class="page-description">In-depth analysis of AI tools, including features, pros, cons, and use cases to help you make informed decisions</p>
  </header>
  
  <div class="post-list">
    {%- for post in collections.reviews | reverse -%}
      <div class="post-card">
        {% if post.data.featured_image %}
  <img src="{{ post.data.featured_image | url }}" alt="{{ post.data.title }}" class="post-card-image">
  {% endif %}
        <div class="post-card-content">
          <h2 class="post-card-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          <div class="post-card-excerpt">{{ post.data.description }}</div>
          <div class="post-card-meta">
            <time datetime="{{ post.date | dateIso }}">{{ post.date | dateDisplay }}</time>
            {% if post.data.tags %}
              <div class="post-card-tags">
                {% for tag in post.data.tags %}
                  <a href="/tags/{{ tag | slug }}/" class="post-tag">{{ tag }}</a>
                {% endfor %}
              </div>
            {% endif %}
          </div>
        </div>
      </div>
    {%- endfor -%}
  </div>
  
  {% if collections.reviews.length === 0 %}
    <div class="empty-state">
      <p>No reviews available yet. Stay tuned!</p>
    </div>
  {% endif %}
</div>

<style>
  .reviews-page {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 0;
  }
  
  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .page-header h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: var(--primary-color);
  }
  
  .page-description {
    font-size: 1.1rem;
    color: var(--gray-color);
    max-width: 800px;
    margin: 0 auto;
  }
  
  .empty-state {
    text-align: center;
    padding: 3rem 0;
    color: var(--gray-color);
    font-size: 1.1rem;
  }
</style> 